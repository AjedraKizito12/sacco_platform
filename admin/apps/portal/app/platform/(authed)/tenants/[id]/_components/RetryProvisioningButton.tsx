"use client";
import type { TenantOut } from "@sacco/schemas";
// Replaced with the maker-checker implementation in the next commit (SP13-T7).
export function RetryProvisioningButton(_props: { tenant: TenantOut }) {
  return null;
}
